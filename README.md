# Zomathon - CSAO Recommendation System

## Dataset Generation
Run the following to generate the synthetic dataset:

This will create the full dataset inside `csao_data/`.

## Baseline Model
Open `csao_model_baseline.ipynb` to train the logistic regression baseline and compute ranking metrics.

